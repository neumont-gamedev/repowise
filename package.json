{
  "name": "repository-intelligence",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "build": "node build.js",
    "deploy": "npm run build && firebase deploy --only functions:accountApi,firestore:rules,hosting --project repowise-neumont",
    "pretest": "npm run build"
  },
  "engines": {
    "node": ">=20.17"
  }
}